<?xml version="1.0" encoding="utf-8"?>
<hibernate-configuration xmlns="urn:nhibernate-configuration-2.2">
    <session-factory>
        <!-- Database connection settings -->
        <property name="connection.provider">NHibernate.Connection.DriverConnectionProvider</property>
        <property name="connection.driver_class">NHibernate.Driver.MySqlDataDriver</property>
        <property name="connection.connection_string">
            Server=localhost;Port=3308;Database=NHibernateDb;Uid=user;Pwd=password;
        </property>

        <!-- Mapping files -->
        <!-- Add mappings here if using XML mapping files -->
        <!-- <mapping assembly="YourAssemblyName" /> -->

        <!-- Show SQL statements in the console -->
        <property name="show_sql">true</property>

        <!-- Dialect for MariaDB -->
        <property name="dialect">NHibernate.Dialect.MySQL57Dialect</property>
    </session-factory>
</hibernate-configuration>
